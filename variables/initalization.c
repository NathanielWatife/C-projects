// first the header file
#include <stdio.h>

int main()
{
    // here is the first assigned value
    int var = 3;
    // here we are reassigning the var to a new value
    var = 4;

    printf("%d", var);
    return 0;
}